# ── Application ──────────────────────────────────────────────
SECRET_KEY=change-me-to-a-random-secret-key

# ── Database ─────────────────────────────────────────────────
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=password
DB_NAME=flask_api_db

# ── Mail ─────────────────────────────────────────────────────
# port 587 → STARTTLS (recommended for Gmail)
# port 465 → implicit SSL
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_TIMEOUT=10
MAIL_USERNAME=your@email.com
MAIL_PASSWORD=your-app-password

# ── Storage ──────────────────────────────────────────────────
# 'local' (default) or 's3'
STORAGE_DRIVER=local

# S3 / S3-compatible (required when STORAGE_DRIVER=s3)
STORAGE_S3_BUCKET=my-bucket
STORAGE_S3_REGION=us-east-1
STORAGE_S3_ACCESS_KEY=AKIAIOSFODNN7EXAMPLE
STORAGE_S3_SECRET_KEY=wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY
STORAGE_S3_ENDPOINT=          # optional: MinIO / R2 / Spaces endpoint
STORAGE_S3_PUBLIC_URL=        # optional: CDN base URL

