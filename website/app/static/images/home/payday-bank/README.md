Drop PAYDAY BANK screenshots in this folder (`.png`, `.jpg`, `.jpeg`, or `.webp`).

They're picked up automatically on the next request — no code changes needed.
Sorted alphabetically, so prefix with numbers if order matters (`01-login.png`,
`02-dashboard.png`, ...). When the folder is empty, the home page shows a
"coming soon" placeholder instead of the gallery.
